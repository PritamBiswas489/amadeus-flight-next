/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['flightaware.com', 'pics.avs.io','daisycon.io','assets.duffel.com','localhost','mitigate-backend.pritamnew.aqualeafitsol.com'],
  },
}

module.exports = nextConfig
